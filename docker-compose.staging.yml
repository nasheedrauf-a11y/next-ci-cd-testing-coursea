services:
  nextjs-staging:
    build: .
    container_name: nextjs-staging
    environment:
      - NEXT_PUBLIC_APP_NAME=Staging Environment
      - NEXT_PUBLIC_API_URL=https://staging-api.example.com
    ports:
      - "3001:3000"
